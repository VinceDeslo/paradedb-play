version: "3.8"

services:
  paradedb:
    image: paradedb/paradedb:latest
    container_name: paradedb
    ports:
      - "6543:5432"
    env_file:
      - .env
    volumes:
      - paradedb_data:/var/lib/postgresql/data
volumes:
  paradedb_data:
